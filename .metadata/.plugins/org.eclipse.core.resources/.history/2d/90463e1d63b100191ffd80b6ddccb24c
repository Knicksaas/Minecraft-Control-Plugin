package com.nte.mc.controlplugin.commands;

import org.bukkit.command.Command;
import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;

public class HealthCommands {
	
	public static void heal(CommandSender sender, Command cmd, String label, String[] args) {
		if(sender instanceof Player) {
			Player p = (Player) sender;s
		} else {
			CommandRooter.sendPlayerOnlyCmdMsg();
		}
	}

}
